import "danda.rd" as dada
import "#io"

fun empty(a: number, b: number, c: number): number {
    return a
}

fun danda(n: number, a: number): number {
    {
        let sedm = 7
    }
    return a
}

fun main(): number {
    io.println("hello world")
}